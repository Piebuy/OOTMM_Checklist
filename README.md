# put spoiler log in main folder and rename it "spoiler_log.txt"

# If there is no location.json, it will create a "new_spoiler_log.txt" and use that to create the custom location.json

# location.json is your savefile. It will also save a location.json to the backupfolder except when you press reset on the website